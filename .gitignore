__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/
# Secrets — never commit
.env
.env.*
.env.test
*.env

# Generated eval artifacts
eval_set.jsonl

# Test model (root-level only — a bare "demo/" also matched client/public/demo/,
# which holds committed static assets for the frontend's demo flow)
/demo/
.vscode/

# Subagent-driven-development scratch workspace (ledgers, briefs, review packages)
.superpowers/